== README

A simple recipe manager built in Ruby on Rails, using Bootstrap.

To get started:
- Run 'brew services start postgresql' in terminal.
- Run 'bundle install'.
- Run 'rails s' to start the server.
